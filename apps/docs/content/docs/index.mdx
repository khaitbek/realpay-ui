---
title: Button 
description: Displays a button or a component that looks like a button 
---

import { Button } from "@hayitbek/realpay-ui-button"
import { Callout } from 'fumadocs-ui/components/callout'

<Tabs items={["preview","code"]}>
<Tab value="preview">
   
        <Button>
    Click me! 
        </Button>
    
</Tab>
<Tab value="code">
    ```tsx
    import {Button} from "@hayitbek/realpay-ui-button"

    export const DemoButton = () => {
        return <Button>
    Button 
        </Button>
    }
    ```
</Tab>
</Tabs>

### Installation
<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```shell
        npm install @hayitbek/realpay-ui-button
    ```
  </Tab>
  <Tab value="pnpm">
    ```shell
        pnpm install @hayitbek/realpay-ui-button
    ```
  </Tab>
  <Tab value="yarn">
    ```shell
        yarn add @hayitbek/realpay-ui-button
    ```
  </Tab>
<Tab value="bun">
    ```shell
        bun add @hayitbek/realpay-ui-button
    ```
  </Tab>
</ Tabs>

### Usage
```tsx
import {Button} from "@hayitbek/realpay-ui-button"
```

```tsx
<Button>
    Click me!
</Button>
```

### Link
You can use the buttonVariants helper to create a link that looks like a button.

```tsx
import { buttonVariants } from "@/components/ui/button"
<Link className={buttonVariants({ variant: "outline" })}>Click here</Link>
```

Alternatively, you can set the asChild parameter and nest the link component.

```tsx
<Button asChild>
  <Link href="/login">Login</Link>
</Button>
```

### Examples
#### Primary
<Tabs items={["Preview","Code"]}>
<Tab value="Preview">
    <Button>Primary button</Button>
</Tab> 
<Tab value="Code">
   ```tsx
    import {Button} from "@hayitbek/realpay-ui-button"

    export const PrimaryButtonDemo = () => {
        return <Button>Primary button</Button>
    }
   ``` 
</Tab> 
</Tabs>
#### Secondary 

<Tabs items={["Preview","Code"]}>
<Tab value="Preview">
    <Button variant="secondary">Secondary button</Button>
</Tab> 
<Tab value="Code">
   ```tsx
    import {Button} from "@hayitbek/realpay-ui-button"

    export const SecondaryButtonDemo = () => {
        return <Button>Secondary button</Button>
    }
   ``` 
</Tab> 
</Tabs>

#### Ghost
<Tabs items={["Preview","Code"]}>
<Tab value="Preview">
    <Button variant="ghost">Ghost button</Button>
</Tab> 
<Tab value="Code">
   ```tsx
    import {Button} from "@hayitbek/realpay-ui-button"

    export const GhostButtonDemo = () => {
        return <Button>Ghost button</Button>
    }
   ``` 
</Tab> 
</Tabs>

#### Outline 
<Tabs items={["Preview","Code"]}>
<Tab value="Preview">
    <Button variant="outline">Outline button</Button>
</Tab> 
<Tab value="Code">
   ```tsx
    import {Button} from "@hayitbek/realpay-ui-button"

    export const GhostButtonDemo = () => {
        return <Button>Ghost button</Button>
    }
   ``` 
</Tab> 
</Tabs>